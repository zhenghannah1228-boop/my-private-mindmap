/**
 * 大厂黑话看板:以收集互联网大厂「黑话」为核心的语料库。
 * 每条 = 黑话词 → 「人话」翻译 + 例句 + 分类。可分类浏览、搜索、导入扩充。
 */

export interface Jargon {
  id: string;
  /** 黑话词 */
  term: string;
  /** 分类(见 JARGON_CATEGORIES) */
  category: string;
  /** 「人话」:它其实是什么意思 */
  plain: string;
  /** 例句(会议高频用法) */
  example?: string;
  /** 可选:字面/直译 */
  literal?: string;
}

export const JARGON_CATEGORIES = [
  '战略愿景',
  '组织协作',
  '增长运营',
  '效率提升',
  '万能动词',
  '画饼鸡汤',
] as const;

/** 「复制采集指令」复制这段:交给任意 AI 产出一批黑话词条 JSON */
export const JARGON_PROMPT = `大厂黑话 · 采集指令

请帮我采集一批互联网大厂的「黑话 / 行话」,做成语料库。风格:真实、常见、带点会心的吐槽,但「人话」翻译要准确,不要瞎编词。

分类(轮换着来,尽量覆盖):战略愿景 / 组织协作 / 增长运营 / 效率提升 / 万能动词 / 画饼鸡汤。

请输出一个 JSON 数组,每个元素:
{
  "id": "<唯一英文短id,如 fu-neng>",
  "term": "<黑话词,如 赋能>",
  "category": "<上面分类之一>",
  "plain": "<「人话」:它其实是什么意思,1–2 句,可带一点吐槽>",
  "example": "<一句会议里的典型例句>",
  "literal": "<可选:字面/直译>"
}

一次给 8–15 条,别和常见词重复太多;拿不准含义的就别收。`;
